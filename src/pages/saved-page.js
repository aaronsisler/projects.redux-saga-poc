import React from "react";
import SavedContainer from "../containers/saved-container";

export default class SavedPage extends React.Component {
  render() {
    return (
      <main className="saved-page">
        <SavedContainer />
      </main>
    );
  }
}
